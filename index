<!doctype html>
<html lang="es">
 <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Explorador interdimensional de personajes de Rick and Morty con diseño futurista">
  <meta name="keywords" content="Rick and Morty, personajes, API, explorador, portal">
  <meta name="author" content="Tu Nombre">
  <title>🛸 Rick and Morty Explorer - Explorador Interdimensional</title><!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg width='128' height='128' viewBox='0 0 128 128' xmlns='http://www.w3.org/2000/svg'%3E%3Cdefs%3E%3CradialGradient id='portalGrad' cx='50%25' cy='50%25' r='50%25'%3E%3Cstop offset='0%25' style='stop-color:%2300ff9d;stop-opacity:1' /%3E%3Cstop offset='30%25' style='stop-color:%2300b8ff;stop-opacity:1' /%3E%3Cstop offset='60%25' style='stop-color:%23ff00ff;stop-opacity:1' /%3E%3Cstop offset='100%25' style='stop-color:%23001eff;stop-opacity:1' /%3E%3C/radialGradient%3E%3Cfilter id='glow'%3E%3CfeGaussianBlur stdDeviation='4' result='coloredBlur'/%3E%3CfeMerge%3E%3CfeMergeNode in='coloredBlur'/%3E%3CfeMergeNode in='SourceGraphic'/%3E%3C/feMerge%3E%3C/filter%3E%3C/defs%3E%3Ccircle cx='64' cy='64' r='60' fill='%230a0e27'/%3E%3Ccircle cx='64' cy='64' r='50' fill='url(%23portalGrad)' opacity='0.8' filter='url(%23glow)'/%3E%3Ccircle cx='64' cy='64' r='40' fill='none' stroke='%2300ff9d' stroke-width='2' opacity='0.6'/%3E%3Ccircle cx='64' cy='64' r='30' fill='none' stroke='%2300b8ff' stroke-width='2' opacity='0.6'/%3E%3Ccircle cx='64' cy='64' r='20' fill='none' stroke='%23ff00ff' stroke-width='2' opacity='0.6'/%3E%3Cellipse cx='64' cy='58' rx='22' ry='8' fill='%2300ff9d' filter='url(%23glow)'/%3E%3Cellipse cx='64' cy='54' rx='14' ry='10' fill='%2300b8ff'/%3E%3Ccircle cx='64' cy='50' r='6' fill='%23ff00ff'/%3E%3Ccircle cx='52' cy='60' r='2' fill='%23ffff00' opacity='0.9'/%3E%3Ccircle cx='64' cy='62' r='2' fill='%23ffff00' opacity='0.9'/%3E%3Ccircle cx='76' cy='60' r='2' fill='%23ffff00' opacity='0.9'/%3E%3Ccircle cx='25' cy='25' r='2' fill='%23ffffff' opacity='0.8'/%3E%3Ccircle cx='100' cy='30' r='1.5' fill='%23ffffff' opacity='0.7'/%3E%3Ccircle cx='105' cy='85' r='2' fill='%23ffffff' opacity='0.8'/%3E%3Ccircle cx='30' cy='95' r='1.5' fill='%23ffffff' opacity='0.7'/%3E%3Ccircle cx='90' cy='15' r='1' fill='%23ffffff' opacity='0.6'/%3E%3C/svg%3E"><!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script><!-- Fuentes -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&amp;display=swap" rel="stylesheet">
  <style>
        body {
            box-sizing: border-box;
        }
        
        /* Animaciones */
        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            25% { transform: translateY(-20px) rotate(-3deg); }
            50% { transform: translateY(-40px) rotate(0deg); }
            75% { transform: translateY(-20px) rotate(3deg); }
        }

        @keyframes pulse-glow {
            0%, 100% { 
                box-shadow: 0 0 40px rgba(0, 255, 157, 0.5), 0 0 80px rgba(0, 184, 255, 0.4), inset 0 0 30px rgba(0, 255, 157, 0.3);
            }
            50% { 
                box-shadow: 0 0 60px rgba(0, 255, 157, 0.9), 0 0 120px rgba(0, 184, 255, 0.7), inset 0 0 50px rgba(0, 255, 157, 0.5);
            }
        }

        @keyframes shimmer {
            0% { background-position: -1000px 0; }
            100% { background-position: 1000px 0; }
        }

        @keyframes neon-flicker {
            0%, 19%, 21%, 23%, 25%, 54%, 56%, 100% {
                text-shadow: 0 0 10px rgba(0, 255, 157, 1), 0 0 20px rgba(0, 255, 157, 0.8), 0 0 30px rgba(0, 255, 157, 0.6), 0 0 40px rgba(0, 184, 255, 0.5), 0 0 70px rgba(0, 184, 255, 0.3);
            }
            20%, 24%, 55% {  
                text-shadow: 0 0 5px rgba(0, 255, 157, 0.5), 0 0 10px rgba(0, 255, 157, 0.3);
            }
        }

        @keyframes border-glow {
            0%, 100% { border-color: rgba(0, 255, 157, 0.8); }
            25% { border-color: rgba(0, 184, 255, 0.8); }
            50% { border-color: rgba(255, 0, 255, 0.8); }
            75% { border-color: rgba(0, 184, 255, 0.8); }
        }

        @keyframes particles-float {
            0% { transform: translateY(0) translateX(0) scale(1); opacity: 0; }
            10% { opacity: 1; }
            90% { opacity: 0.5; }
            100% { transform: translateY(-150px) translateX(100px) scale(0.3); opacity: 0; }
        }

        @keyframes rotate-border {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .float-animation { animation: float 5s ease-in-out infinite; }
        .pulse-glow { animation: pulse-glow 3s ease-in-out infinite; }
        .neon-text { animation: neon-flicker 7s infinite; }

        .character-card {
            position: relative;
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            overflow: hidden;
        }

        .character-card::before {
            content: '';
            position: absolute;
            top: -3px;
            left: -3px;
            right: -3px;
            bottom: -3px;
            background: linear-gradient(60deg, #00ff9d, #00b8ff, #ff00ff, #ffaa00, #00ff9d);
            background-size: 300% 300%;
            border-radius: 14px;
            opacity: 0;
            z-index: -1;
            animation: shimmer 4s linear infinite, border-glow 2s linear infinite;
        }

        .character-card:hover::before { opacity: 0.7; }

        .character-card:hover {
            transform: translateY(-15px) scale(1.05) rotate(-1deg);
            box-shadow: 0 25px 50px rgba(0, 255, 157, 0.4), 0 0 100px rgba(0, 184, 255, 0.3), inset 0 0 20px rgba(0, 255, 157, 0.1);
        }

        .portal-bg {
            background: radial-gradient(circle at center, #00ff9d 0%, #00b8ff 25%, #ff00ff 50%, #ffaa00 75%, #001eff 100%);
            position: relative;
            overflow: hidden;
        }

        .portal-bg::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(from 0deg, transparent, rgba(255, 255, 255, 0.3), transparent 30%);
            animation: rotate-border 4s linear infinite;
        }

        .portal-bg::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, transparent 30%, rgba(255, 255, 255, 0.4) 50%, transparent 70%);
            background-size: 200% 200%;
            animation: shimmer 3s infinite;
        }

        .gradient-text {
            background: linear-gradient(135deg, #00ff9d 0%, #00b8ff 30%, #ff00ff 60%, #ffaa00 100%);
            background-size: 300% 300%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: shimmer 5s ease-in-out infinite;
        }

        .glass-effect {
            background: rgba(31, 41, 55, 0.3);
            backdrop-filter: blur(25px);
            border: 2px solid rgba(0, 255, 157, 0.3);
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4), inset 0 0 30px rgba(0, 255, 157, 0.08);
        }

        .status-alive {
            background: linear-gradient(135deg, #00ff9d 0%, #00cc7a 100%);
            box-shadow: 0 0 25px rgba(0, 255, 157, 0.8), 0 0 50px rgba(0, 255, 157, 0.4);
            animation: pulse-glow 2s ease-in-out infinite;
        }

        .status-dead {
            background: linear-gradient(135deg, #ff4444 0%, #cc0000 100%);
            box-shadow: 0 0 25px rgba(255, 68, 68, 0.8), 0 0 50px rgba(255, 68, 68, 0.4);
        }

        .status-unknown {
            background: linear-gradient(135deg, #9ca3af 0%, #6b7280 100%);
            box-shadow: 0 0 20px rgba(156, 163, 175, 0.5);
        }

        .custom-scrollbar::-webkit-scrollbar { width: 12px; }
        .custom-scrollbar::-webkit-scrollbar-track {
            background: rgba(15, 23, 42, 0.8);
            border-radius: 10px;
            border: 1px solid rgba(0, 255, 157, 0.2);
        }
        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #00ff9d 0%, #00b8ff 50%, #ff00ff 100%);
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 255, 157, 0.6);
        }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #ff00ff 0%, #00b8ff 50%, #00ff9d 100%);
            box-shadow: 0 0 25px rgba(0, 255, 157, 0.9);
        }

        .loading-spinner {
            border: 5px solid rgba(31, 41, 55, 0.3);
            border-top: 5px solid #00ff9d;
            border-right: 5px solid #00b8ff;
            border-bottom: 5px solid #ff00ff;
            border-radius: 50%;
            width: 60px;
            height: 60px;
            animation: spin 0.8s linear infinite;
            box-shadow: 0 0 40px rgba(0, 255, 157, 0.5), inset 0 0 20px rgba(0, 255, 157, 0.3);
        }

        .stat-bar {
            background: linear-gradient(90deg, #00ff9d 0%, #00b8ff 40%, #ff00ff 70%, #ffaa00 100%);
            box-shadow: 0 0 25px rgba(0, 255, 157, 0.5), 0 5px 15px rgba(0, 0, 0, 0.3);
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
        }

        .stat-bar::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: shimmer 2s infinite;
        }

        .stat-bar:hover {
            box-shadow: 0 0 35px rgba(0, 255, 157, 0.9), 0 5px 25px rgba(0, 0, 0, 0.5);
            transform: scaleY(1.3) scaleX(1.02);
        }

        .favorite-badge {
            background: linear-gradient(135deg, rgba(0, 255, 157, 0.4) 0%, rgba(0, 184, 255, 0.4) 50%, rgba(255, 0, 255, 0.4) 100%);
            border: 2px solid #00ff9d;
            box-shadow: 0 0 25px rgba(0, 255, 157, 0.4), inset 0 0 15px rgba(0, 255, 157, 0.2);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .favorite-badge:hover {
            background: linear-gradient(135deg, rgba(0, 255, 157, 0.6) 0%, rgba(0, 184, 255, 0.6) 50%, rgba(255, 0, 255, 0.6) 100%);
            box-shadow: 0 0 40px rgba(0, 255, 157, 0.7), inset 0 0 25px rgba(0, 255, 157, 0.3);
            transform: scale(1.15) rotate(-3deg);
            border-color: #ff00ff;
        }

        .detail-card {
            transition: all 0.3s ease;
        }

        .detail-card:hover {
            transform: translateX(-5px);
            box-shadow: 0 10px 30px rgba(0, 255, 157, 0.3);
        }

        .input-glow:focus {
            box-shadow: 0 0 30px rgba(0, 255, 157, 0.4), inset 0 0 15px rgba(0, 255, 157, 0.1);
            border-color: #00ff9d;
            outline: none;
        }

        .star-button {
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            filter: drop-shadow(0 0 5px rgba(255, 215, 0, 0.5));
            cursor: pointer;
            background: transparent;
            border: none;
        }

        .star-button:hover {
            transform: scale(1.3) rotate(180deg);
            filter: drop-shadow(0 0 15px rgba(255, 215, 0, 0.9));
        }

        .background-particles {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            pointer-events: none;
            z-index: 0;
        }

        .particle {
            position: absolute;
            width: 3px;
            height: 3px;
            background: #00ff9d;
            border-radius: 50%;
            animation: particles-float 8s linear infinite;
            box-shadow: 0 0 10px #00ff9d, 0 0 20px #00ff9d;
        }

        @media (max-width: 768px) {
            .character-card { transform: none !important; }
            .character-card:hover { transform: translateY(-10px) scale(1.02) !important; }
        }
    </style>
  <style>@view-transition { navigation: auto; }</style>
  <script src="/_sdk/data_sdk.js" type="text/javascript"></script>
  <script src="/_sdk/element_sdk.js" type="text/javascript"></script>
 </head>
 <body class="bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 min-h-full text-white font-['Inter']"><!-- Partículas de fondo -->
  <div class="background-particles" id="particles"></div><!-- Contenedor principal -->
  <div class="relative z-10 min-h-full"><!-- Header -->
   <header class="glass-effect py-8 px-4 sticky top-0 z-50 mb-8">
    <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-4"><!-- Logo y título -->
     <div class="flex items-center gap-4">
      <div class="portal-bg w-20 h-20 rounded-full float-animation flex items-center justify-center"><span class="text-4xl">🛸</span>
      </div>
      <div>
       <h1 class="text-4xl md:text-5xl font-black neon-text">Rick and Morty Explorer</h1>
       <p class="text-sm md:text-base text-gray-400 mt-1">🌌 Explora el multiverso de personajes 🌌</p>
      </div>
     </div><!-- Contador de favoritos -->
     <div class="favorite-badge rounded-full px-6 py-3 flex items-center gap-2"><span class="text-2xl">⭐</span> <span class="font-bold text-lg">Favoritos: <span id="favCount">0</span></span>
     </div>
    </div><!-- Barra de favoritos -->
    <div id="favoritesBar" class="max-w-7xl mx-auto mt-6 flex gap-3 overflow-x-auto pb-3 custom-scrollbar"></div>
   </header><!-- Main Content -->
   <main class="max-w-7xl mx-auto px-4 pb-12"><!-- Filtros -->
    <section class="glass-effect rounded-2xl p-6 mb-8">
     <div class="grid grid-cols-1 md:grid-cols-2 gap-4"><!-- Búsqueda -->
      <div><label for="searchInput" class="block text-sm font-semibold mb-2 gradient-text">🔍 Buscar por Nombre</label> <input type="text" id="searchInput" placeholder="Escribe un nombre..." class="w-full px-4 py-3 bg-gray-800/50 border-2 border-gray-700 rounded-lg text-white input-glow transition-all">
      </div><!-- Filtro de estado -->
      <div><label for="statusFilter" class="block text-sm font-semibold mb-2 gradient-text">💚 Filtrar por Estado</label> <select id="statusFilter" class="w-full px-4 py-3 bg-gray-800/50 border-2 border-gray-700 rounded-lg text-white input-glow transition-all cursor-pointer"> <option value="">Todos los Estados</option> <option value="alive">Vivo 💚</option> <option value="dead">Muerto 💀</option> <option value="unknown">Desconocido ❓</option> </select>
      </div>
     </div>
    </section><!-- Grid de personajes y panel de detalles -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8"><!-- Lista de personajes -->
     <div class="lg:col-span-2">
      <div id="charactersGrid" class="grid grid-cols-1 sm:grid-cols-2 gap-6"></div><!-- Estado de carga -->
      <div id="loadingState" class="flex flex-col items-center justify-center py-20">
       <div class="loading-spinner mb-4"></div>
       <p class="text-xl font-semibold gradient-text">Cargando personajes del multiverso...</p>
      </div><!-- Estado vacío -->
      <div id="emptyState" class="hidden flex-col items-center justify-center py-20 glass-effect rounded-2xl"><span class="text-8xl mb-4">🔍</span>
       <p class="text-2xl font-bold gradient-text">No se encontraron personajes</p>
       <p class="text-gray-400 mt-2">Intenta con otro nombre o filtro</p>
      </div>
     </div><!-- Panel de detalles -->
     <div class="lg:col-span-1">
      <div id="detailsPanel" class="glass-effect rounded-2xl p-6 sticky top-28 max-h-[calc(100vh-8rem)] overflow-y-auto custom-scrollbar">
       <div class="flex flex-col items-center justify-center h-full text-center py-12"><span class="text-6xl mb-4">👆</span>
        <p class="text-xl font-semibold gradient-text">Selecciona un personaje</p>
        <p class="text-gray-400 mt-2 text-sm">Haz clic en cualquier tarjeta para ver detalles completos</p>
       </div>
      </div>
     </div>
    </div><!-- Estadísticas -->
    <section class="mt-12 glass-effect rounded-2xl p-8">
     <h2 class="text-3xl font-black gradient-text mb-6 text-center">📊 Estadísticas del Multiverso</h2>
     <div class="grid grid-cols-1 md:grid-cols-2 gap-8"><!-- Especies -->
      <div>
       <h3 class="text-xl font-bold text-cyan-400 mb-4">🧬 Por Especies</h3>
       <div id="speciesStats" class="space-y-3"></div>
      </div><!-- Tipos -->
      <div>
       <h3 class="text-xl font-bold text-purple-400 mb-4">🏷️ Por Tipos</h3>
       <div id="typesStats" class="space-y-3"></div>
      </div>
     </div>
    </section>
   </main><!-- Footer -->
   <footer class="glass-effect py-6 mt-12">
    <div class="max-w-7xl mx-auto px-4 text-center">
     <p class="text-gray-400">Creado con 💚 usando la <a href="https://rickandmortyapi.com/" target="_blank" rel="noopener noreferrer" class="gradient-text font-semibold hover:underline"> Rick and Morty API </a></p>
     <p class="text-gray-500 text-sm mt-2">🛸 Wubba Lubba Dub Dub! 🛸</p>
    </div>
   </footer>
  </div>
  <script>
        // Estado global
        let allCharacters = [];
        let favorites = new Set();

        // Elementos del DOM
        const charactersGrid = document.getElementById('charactersGrid');
        const detailsPanel = document.getElementById('detailsPanel');
        const searchInput = document.getElementById('searchInput');
        const statusFilter = document.getElementById('statusFilter');
        const loadingState = document.getElementById('loadingState');
        const emptyState = document.getElementById('emptyState');
        const favCount = document.getElementById('favCount');
        const favoritesBar = document.getElementById('favoritesBar');

        // Generar partículas de fondo
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            for (let i = 0; i < 50; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 8 + 's';
                particle.style.animationDuration = (Math.random() * 4 + 6) + 's';
                particlesContainer.appendChild(particle);
            }
        }

        // Obtener personajes desde la API
        async function fetchCharacters() {
            try {
                loadingState.classList.remove('hidden');
                emptyState.classList.add('hidden');
                charactersGrid.innerHTML = '';

                const response = await fetch('https://rickandmortyapi.com/api/character');
                const data = await response.json();
                allCharacters = data.results;

                displayCharacters(allCharacters);
                updateStatistics(allCharacters);
            } catch (error) {
                console.error('Error al cargar personajes:', error);
                charactersGrid.innerHTML = '<p class="text-red-400 text-center col-span-full">Error al cargar personajes. Por favor, recarga la página.</p>';
            } finally {
                loadingState.classList.add('hidden');
            }
        }

        // Mostrar personajes
        function displayCharacters(characters) {
            charactersGrid.innerHTML = '';
            
            if (characters.length === 0) {
                emptyState.classList.remove('hidden');
                emptyState.classList.add('flex');
                return;
            }

            emptyState.classList.add('hidden');

            characters.forEach(character => {
                const card = createCharacterCard(character);
                charactersGrid.appendChild(card);
            });
        }

        // Crear tarjeta de personaje
        function createCharacterCard(character) {
            const card = document.createElement('div');
            card.className = 'character-card glass-effect rounded-xl overflow-hidden cursor-pointer';
            
            const isFavorite = favorites.has(character.id);
            const statusClass = character.status.toLowerCase() === 'alive' ? 'status-alive' : 
                               character.status.toLowerCase() === 'dead' ? 'status-dead' : 'status-unknown';

            card.innerHTML = `
                <div class="relative">
                    <img src="${character.image}" alt="${character.name}" class="w-full h-64 object-cover">
                    <button 
                        class="star-button absolute top-3 right-3 text-3xl"
                        onclick="toggleFavorite(event, ${character.id})"
                    >
                        ${isFavorite ? '⭐' : '☆'}
                    </button>
                    <div class="${statusClass} absolute bottom-3 left-3 px-3 py-1 rounded-full text-xs font-bold flex items-center gap-2">
                        <span class="w-2 h-2 bg-white rounded-full"></span>
                        ${character.status}
                    </div>
                </div>
                <div class="p-4">
                    <h3 class="text-xl font-bold mb-2 gradient-text">${character.name}</h3>
                    <p class="text-gray-400 text-sm mb-1">🧬 ${character.species}</p>
                    <p class="text-gray-400 text-sm">⚧ ${character.gender}</p>
                </div>
            `;

            card.addEventListener('click', (e) => {
                if (!e.target.closest('.star-button')) {
                    showCharacterDetails(character);
                }
            });

            return card;
        }

        // Mostrar detalles del personaje
        async function showCharacterDetails(character) {
            detailsPanel.innerHTML = `
                <div class="flex flex-col items-center">
                    <div class="loading-spinner mb-4"></div>
                    <p class="text-sm text-gray-400">Cargando detalles...</p>
                </div>
            `;

            try {
                const [originData, locationData, episodeData] = await Promise.all([
                    character.origin.url ? fetch(character.origin.url).then(r => r.json()) : Promise.resolve(null),
                    character.location.url ? fetch(character.location.url).then(r => r.json()) : Promise.resolve(null),
                    character.episode[0] ? fetch(character.episode[0]).then(r => r.json()) : Promise.resolve(null)
                ]);

                const statusClass = character.status.toLowerCase() === 'alive' ? 'status-alive' : 
                                   character.status.toLowerCase() === 'dead' ? 'status-dead' : 'status-unknown';

                detailsPanel.innerHTML = `
                    <div class="text-center">
                        <img src="${character.image}" alt="${character.name}" class="w-48 h-48 rounded-full mx-auto mb-4 pulse-glow border-4 border-cyan-400">
                        <h2 class="text-3xl font-black gradient-text mb-2">${character.name}</h2>
                        <div class="${statusClass} inline-block px-4 py-2 rounded-full text-sm font-bold mb-6">
                            ${character.status}
                        </div>
                    </div>

                    <div class="space-y-4 mt-6">
                        <div class="detail-card bg-gray-800/30 p-4 rounded-lg border border-cyan-400/30">
                            <p class="text-cyan-400 font-semibold mb-1">🧬 Especie</p>
                            <p class="text-white">${character.species}</p>
                        </div>

                        <div class="detail-card bg-gray-800/30 p-4 rounded-lg border border-purple-400/30">
                            <p class="text-purple-400 font-semibold mb-1">⚧ Género</p>
                            <p class="text-white">${character.gender}</p>
                        </div>

                        ${character.type ? `
                        <div class="detail-card bg-gray-800/30 p-4 rounded-lg border border-pink-400/30">
                            <p class="text-pink-400 font-semibold mb-1">🏷️ Tipo</p>
                            <p class="text-white">${character.type}</p>
                        </div>
                        ` : ''}

                        <div class="detail-card bg-gray-800/30 p-4 rounded-lg border border-green-400/30">
                            <p class="text-green-400 font-semibold mb-1">🌍 Origen</p>
                            <p class="text-white">${character.origin.name}</p>
                            ${originData && originData.type ? `<p class="text-gray-400 text-sm mt-1">Tipo: ${originData.type}</p>` : ''}
                            ${originData && originData.dimension ? `<p class="text-gray-400 text-sm">Dimensión: ${originData.dimension}</p>` : ''}
                        </div>

                        <div class="detail-card bg-gray-800/30 p-4 rounded-lg border border-blue-400/30">
                            <p class="text-blue-400 font-semibold mb-1">📍 Ubicación Actual</p>
                            <p class="text-white">${character.location.name}</p>
                            ${locationData && locationData.type ? `<p class="text-gray-400 text-sm mt-1">Tipo: ${locationData.type}</p>` : ''}
                        </div>

                        ${episodeData ? `
                        <div class="detail-card bg-gray-800/30 p-4 rounded-lg border border-yellow-400/30">
                            <p class="text-yellow-400 font-semibold mb-1">📺 Primer Episodio</p>
                            <p class="text-white">${episodeData.name}</p>
                            <p class="text-gray-400 text-sm mt-1">${episodeData.episode} - ${episodeData.air_date}</p>
                        </div>
                        ` : ''}

                        <div class="detail-card bg-gray-800/30 p-4 rounded-lg border border-gray-400/30">
                            <p class="text-gray-400 font-semibold mb-1">📅 Creado</p>
                            <p class="text-white">${new Date(character.created).toLocaleDateString('es-ES')}</p>
                        </div>
                    </div>
                `;
            } catch (error) {
                console.error('Error al cargar detalles:', error);
                detailsPanel.innerHTML = '<p class="text-red-400 text-center">Error al cargar detalles</p>';
            }
        }

        // Toggle favorito
        function toggleFavorite(event, characterId) {
            event.stopPropagation();
            
            if (favorites.has(characterId)) {
                favorites.delete(characterId);
            } else {
                favorites.add(characterId);
            }
            
            updateFavoritesUI();
            displayCharacters(filterCharacters());
        }

        // Actualizar UI de favoritos
        function updateFavoritesUI() {
            favCount.textContent = favorites.size;
            
            favoritesBar.innerHTML = '';
            
            if (favorites.size === 0) {
                favoritesBar.innerHTML = '<p class="text-gray-400 text-sm">No hay favoritos aún. Haz clic en la estrella (☆) de cualquier personaje.</p>';
                return;
            }

            favorites.forEach(id => {
                const character = allCharacters.find(c => c.id === id);
                if (character) {
                    const badge = document.createElement('div');
                    badge.className = 'favorite-badge rounded-lg px-4 py-2 flex items-center gap-3 cursor-pointer flex-shrink-0';
                    badge.innerHTML = `
                        <img src="${character.image}" alt="${character.name}" class="w-10 h-10 rounded-full">
                        <span class="font-semibold">${character.name}</span>
                        <button onclick="toggleFavorite(event, ${character.id})" class="text-xl hover:scale-125 transition-transform">❌</button>
                    `;
                    badge.addEventListener('click', (e) => {
                        if (!e.target.closest('button')) {
                            showCharacterDetails(character);
                        }
                    });
                    favoritesBar.appendChild(badge);
                }
            });
        }

        // Filtrar personajes
        function filterCharacters() {
            const searchTerm = searchInput.value.toLowerCase();
            const statusValue = statusFilter.value.toLowerCase();

            return allCharacters.filter(character => {
                const matchesSearch = character.name.toLowerCase().includes(searchTerm);
                const matchesStatus = !statusValue || character.status.toLowerCase() === statusValue;
                return matchesSearch && matchesStatus;
            });
        }

        // Debounce para búsqueda
        let searchTimeout;
        searchInput.addEventListener('input', () => {
            clearTimeout(searchTimeout);
            searchTimeout = setTimeout(() => {
                displayCharacters(filterCharacters());
            }, 300);
        });

        // Filtro de estado
        statusFilter.addEventListener('change', () => {
            displayCharacters(filterCharacters());
        });

        // Actualizar estadísticas
        function updateStatistics(characters) {
            const speciesCount = {};
            const typesCount = {};

            characters.forEach(char => {
                speciesCount[char.species] = (speciesCount[char.species] || 0) + 1;
                if (char.type) {
                    typesCount[char.type] = (typesCount[char.type] || 0) + 1;
                }
            });

            displayStats('speciesStats', speciesCount, characters.length);
            displayStats('typesStats', typesCount, characters.length);
        }

        // Mostrar estadísticas
        function displayStats(containerId, data, total) {
            const container = document.getElementById(containerId);
            const sorted = Object.entries(data).sort((a, b) => b[1] - a[1]).slice(0, 10);

            container.innerHTML = sorted.map(([name, count]) => {
                const percentage = ((count / total) * 100).toFixed(1);
                return `
                    <div>
                        <div class="flex justify-between mb-1">
                            <span class="text-sm font-medium text-gray-300">${name || 'Sin tipo'}</span>
                            <span class="text-sm text-gray-400">${count} (${percentage}%)</span>
                        </div>
                        <div class="w-full bg-gray-700 rounded-full h-3 overflow-hidden">
                            <div class="stat-bar h-full rounded-full" style="width: ${percentage}%"></div>
                        </div>
                    </div>
                `;
            }).join('');
        }

        // Inicializar aplicación
        document.addEventListener('DOMContentLoaded', () => {
            createParticles();
            fetchCharacters();
            updateFavoritesUI();
        });
    </script>
 <script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'9a448071a2da7efc',t:'MTc2NDEwODg5NS4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>

</html>
